<%= form_for(current_user.participations.find_by(exchange_id: @exchange.id),
		html: {method: :delete}) do |f| %>
		<div><%= hidden_field_tag :exchange_id, @exchange.id %></div>
  <%= f.submit "Leave", class: "btn btn-primary" %>
<% end %>